# Basic screenshot configuration
# Note: UI tests need to be set up in Xcode for full functionality

devices([
  "iPhone 16 Pro Max"
])

languages([
  "en-US"
])

workspace("./ios/weathersunscreenapp.xcworkspace")
scheme("weathersunscreenapp")
output_directory("./fastlane/screenshots/ios")
clear_previous_screenshots(true)
stop_after_first_error(false)
number_of_retries(1)

# Skip building for now, focus on configuration
skip_build_summary(true)
